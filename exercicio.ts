const multiplicaValores = (a: number, b: number) : number => a * b;

const saudacaoUsuario = (nomeUsuario: string) : string => `Olá, ${nomeUsuario}.`;

console.log(multiplicaValores(2, 4));
console.log(saudacaoUsuario("Jorge"));