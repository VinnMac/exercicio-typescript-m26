const multiplicaValores = (a: number, b: number) : number => a * b;

const saldacaoUsuario = (nomeUsuario: string) : string => `Olá, ${nomeUsuario}.`;

console.log(multiplicaValores(2, 4));
console.log(saldacaoUsuario("Jorge"));